apiVersion: v1
kind: Service
metadata:
  name: wisecow
  namespace: wisecow
spec:
  selector:
    app: wisecow
  ports:
    - name: http
      port: 80           # cluster-visible port
      targetPort: 4499   # container port
  type: ClusterIP
